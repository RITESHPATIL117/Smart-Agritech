# Local dev: leave EMPTY (Vite proxies /api → http://localhost:5000)
#
# Production (Amplify env var):
#   Key:   VITE_API_URL
#   Value: https://smart-agritech-env.eba-xxxxx.us-east-1.elasticbeanstalk.com
#   (URL only — do NOT paste "VITE_API_URL=" in the value field)
VITE_API_URL=
